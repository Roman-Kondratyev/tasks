// Задача на слово палиндром. Если слово одинаковое с 2 сторон, то true, в
// противном случае false
// мем -> true
// тест -> false

let a = prompt(`введите слово для проверки на палиндром`);
let b = a.split(``).reverse().join(``);

alert(a === b ? true : false);